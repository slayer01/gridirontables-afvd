name: Build Plugin ZIP

on:
  push:
    branches: ['*']

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version
        run: |
          VERSION=$(grep -oP "Version:\s+\K[0-9]+\.[0-9]+\.[0-9]+" football-data.php | head -1)
          echo "VERSION=${VERSION}" >> "$GITHUB_ENV"
          echo "Plugin version: ${VERSION}"

      - name: Build ZIP
        run: |
          mkdir -p footballdata
          cp -r football-data.php uninstall.php readme.txt README.md includes/ admin/ public/ footballdata/
          zip -r footballdata.zip footballdata/

      - name: Create or update release
        run: |
          TAG="v${VERSION}"
          if gh release view "$TAG" > /dev/null 2>&1; then
            gh release upload "$TAG" footballdata.zip --clobber
          else
            gh release create "$TAG" footballdata.zip \
              --title "FootballData ${VERSION}" \
              --notes "Release ${VERSION}" \
              --latest
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
